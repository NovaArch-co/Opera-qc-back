# Redis Sentinel Configuration for Opera QC
# Monitors Redis master and handles automatic failover

port 26379
bind 0.0.0.0

# Sentinel identification
sentinel myid $(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 40 | head -n 1)

# Monitor the master Redis instance
# Format: sentinel monitor <master-name> <ip> <port> <quorum>
sentinel monitor mymaster redis-master 6379 2

# Authentication
sentinel auth-pass mymaster ${REDIS_PASSWORD:-OperaQC2024!Secure}

# Timeouts and thresholds
sentinel down-after-milliseconds mymaster 5000
sentinel parallel-syncs mymaster 1
sentinel failover-timeout mymaster 10000

# Notification scripts (optional - for alerts)
# sentinel notification-script mymaster /var/redis/notify.sh
# sentinel client-reconfig-script mymaster /var/redis/reconfig.sh

# Deny dangerous commands
sentinel deny-scripts-reconfig yes

# Logging
logfile /var/log/redis/sentinel.log
loglevel notice

# Performance
tcp-keepalive 60
