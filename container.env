# Core (local)
NODE_ENV=development
HOST=0.0.0.0
PORT=8081
SWAGGER_URL=http://app:8081/api/docs
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:8084,http://swagger-ui:8080,http://host.docker.internal:8081,http://host.docker.internal:8082, http://31.184.134.153:8081,http://localhost:8081

# Rate limiting (safe for local load tests)
COMMON_RATE_LIMIT_WINDOW_MS=1000
COMMON_RATE_LIMIT_MAX_REQUESTS=100000

# File server (incoming base; outgoing is derived by replacing 'incoming' → 'outgoing')
# If you have a local/LAN file server, point to that; otherwise keep a reachable dev host.
# Example (LAN): http://192.168.1.115/tmp/two-channel/stream-audio-incoming.php?recfile=
FILE_SERVER_BASE_URL=http://192.168.1.115/tmp/two-channel/stream-audio-incoming.php?recfile=

# Redis / Queue (docker-compose services)
REDIS_HOST=redis
REDIS_PORT=6379
BULL_QUEUE=analyseCalls

# Database (docker-compose service)
POSTGRES_DB=opera_qc_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/opera_qc_db

# JWT (local dev)
JWT_SECRET=dev-secret
JWT_REFRESH_SECRET=dev-refresh-secret
JWT_EXPIRES_IN=1d

# MinIO (docker-compose service)
MINIO_ENDPOINT_UTL=http://minio:9000
MINIO_ACCESS_KEY=minioaccesskey
MINIO_SECRET_KEY=miniosecretkey

POSTGRES_DB=opera_qc
POSTGRES_USER=postgres
POSTGRES_PASSWORD=StrongP@ssw0rd123REDIS_PASSWORD=syfvcPbEeWj5tJOIQqh6kSSHzCdTjGc0P8FQBnfK89w=
REDIS_SENTINEL_HOSTS=redis-sentinel-1:26379,redis-sentinel-2:26379,redis-sentinel-3:26379
REDIS_SENTINEL_NAME=mymaster
